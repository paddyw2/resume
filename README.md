# Resume - Patrick Withams
### A command line Java resume program

To view resume:

 * Clone the repo: <code>git clone https://github.com/paddyw2/resume</code>
 * Change to <code>bin</code> directory
 * Run <code>java Resume</code>
